there is a file called "misc/macros.h" in the header. you can delete it, but it had a macro called printl(message) that just extends to "std::cout<<message<<std::endl".
you may want to replace the macro or just define it
